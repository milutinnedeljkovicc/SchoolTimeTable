<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SchoolTimeTable" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/textDarkGray</item>
        <item name="colorPrimaryVariant">@color/purple_200</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/lightGreen</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Background and surface colors -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/textDarkGray</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/textDarkGray</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/textDarkGray</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/textDarkGray</item>
    </style>

    <style name="timePickerWhite">
        <item name="android:textColorPrimary">@color/white</item> <!-- color for digits -->
        <item name="android:textColor">@color/white</item> <!-- color for colon -->
        <item name="android:colorControlNormal">@color/white</item> <!-- color for (horizontal) delimiters -->
    </style>
</resources>
